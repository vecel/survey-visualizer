/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/survey-visualizer",
  assetPrefix: "/survey-visualizer",
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
